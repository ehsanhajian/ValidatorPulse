# Active chain plugin: ethereum | polkadot (implemented).
# Reserved: cosmos, solana (clear error until implemented).
CHAIN=ethereum

# --- Ethereum ---
# Beacon node HTTP API (required for live Ethereum monitoring)
BEACON_API_URL=http://127.0.0.1:5052

# Numeric indices from your beacon / explorer:
VALIDATOR_INDICES=123456,789012

# BLS pubkeys — the usual "validator address" (0x + 96 hex chars).
VALIDATOR_PUBKEYS=

# --- Polkadot / parachain collators ---
# Substrate HTTP JSON-RPC (required for live collator monitoring)
SUBSTRATE_RPC_URL=http://127.0.0.1:9933

# Comma-separated SS58 collator addresses
COLLATOR_ADDRESSES=

# Optional parachain id (labeling / status only). Leave unset or blank if unused.
# PARACHAIN_ID=2000

# Poll interval in seconds
POLL_INTERVAL_SECONDS=12

# Demo mode (true/false). Also auto-enabled when the chain RPC URL is unset.
DEMO_MODE=true

# Server bind
HOST=127.0.0.1
PORT=3000

# Alert channels (configure only what you use)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

SLACK_WEBHOOK_URL=

DISCORD_WEBHOOK_URL=

WEBHOOK_URL=

PAGERDUTY_ROUTING_KEY=

# Alert thresholds
ALERT_MISSED_ATTESTATIONS=2
ALERT_EFFECTIVENESS_BELOW=95
ALERT_SLASHING_RISK_ABOVE=40
ALERT_DISK_USAGE_ABOVE=85
ALERT_CLOCK_DRIFT_MS=500
